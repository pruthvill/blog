import Post from "@/app/components/Post";

const writing = () => {
  const postData = {
    title: "Don't play lost games.",
    content: (
      <>
   
      </>
    ),
  };

  return <Post {...postData} />;
};

export default writing;
